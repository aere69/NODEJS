// Use backticks (`) when creating strings instead of (') or (").
// With that syntax, you can dynamically add data into a string.

const name = "Max";
const age = 29;
console.log(`My name is ${name} and I am ${age} years old.`);